
       <div class="wrapper">
            
            <div class="sidebar">
                <div class="sidebar-text d-flex flex-column h-75 justify-content-center text-center">
                  
                   <img class="mx-auto d-block w-75 bg-primary img-fluid rounded-circle mb-4 p-3" src='/uploads/{{users.profilePicId}}' alt="Image">
        
                   <a href="/members/edit-pic"><button class="GFG" id="pop">Edit Image</button></a>
         {{!-- Image upload model --}}
         {{!-- / Image upload model --}}
                    <h1 class="font-weight-bold">{{users.firstName}}</h1>
                    <h5>{{users.position}}</h5>
                    <p>{{users.place}}&nbsp;||&nbsp;{{users.country}}</p>
                    <p>{{users.email}}&nbsp;||&nbsp;Ph:{{users.memberPhone}}</p>
                    <p class="">
                        <p>{{users.bio}}</p>
                        <a href="/members/edit-profile" class="text-primary"><ul>Edit Profile</ul> </a>
                    </p>
                
                   
                   <h3>Wallet : ₹ {{users.wallet}}</h3>
                    {{!-- <a href="" class="btn btn-lg btn-block btn-primary mt-auto">Hire Me</a> --}}
                </div>
                <div class="sidebar-icon d-flex flex-column h-100 justify-content-center text-right">
                    <i class="fas fa-2x fa-angle-double-right text-primary"></i>
                </div>
            </div>

            {{!--add post section --}}

    <div class="content mb-5">
 

            <!-- Page Header Start -->
            <div class="container py-5 px-2 bg-primary">
                <div class="row py-5 px-4">
                    <div class="col-sm-6 text-center text-md-left">
                        <h3 class="mb-3 mb-md-0 text-white text-uppercase font-weight-bold">Edit your post</h3>
                    </div>
                    
                </div>
            </div>
            <!-- Page Header End -->


            <!-- Contact Start -->
            <div class="container bg-white pt-3 py-5">
         
                <div class="col-md-12 pb-5">
                    <div class="contact-form">
                        <div id="success"></div>
                        <form action="/api/posts/{{post._id}}" method="POST">
                                <input type="hidden" name="_method" value="PUT">
                             <div class="control-group">
                                <input type="text" name="username" value="{{users.firstName}}" class="form-control" id="name" readonly="readonly" />
                                <p class="help-block text-danger"></p>
                            </div>
                            <div class="control-group">
                                <input type="text" name="title" class="form-control" value="{{post.title}}" id="name" placeholder="Title" />
                                <p class="help-block text-danger"></p>
                            </div>
                            
                            <div class="control-group">
                                <textarea class="form-control" name="desc" rows="14" id="comment-text">{{post.desc}}</textarea>
                                <p class="help-block text-danger"></p>
                            </div>
                            <div class="control-group">
                                <select class="form-control" name="categories">
                                        <option class="hidden" selected disabled>Please select the category</option>
                                        {{#each categories}}
                                        <option>{{name}}</option>
                                       {{/each}}
                                    </select>
                               
                            </div>
                            <div>   
                                <button class="btn btn-primary" type="submit" id="">Edit post</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <!-- Contact End -->


        
        </div>

            {{!-- /add post section --}}
     </div>
        
        <!-- Back to Top -->
        <a href="#" class="back-to-top"><i class="fa fa-angle-double-up"></i></a>
       